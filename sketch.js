// required for p5.js project
function setup() {
  	createCanvas(800, 800);
}

// required for p5.js project
// initiates the branch recursion for drawing the tree
function draw() {
  	background(51);
}